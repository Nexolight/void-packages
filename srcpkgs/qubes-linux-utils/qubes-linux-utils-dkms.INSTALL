# INSTALL

case "$ACTION" in
pre)
	;;
post)
	if [ "$UPDATE" = "no" ]; then
		echo "xen /proc/xen xenfs defaults 0 0" >> /etc/fstab
		update-grub
		dracut -f
	fi
	if [ "$UPDATE" = "yes" ]; then
		update-grub
		dracut -f
	fi
	;;
esac
